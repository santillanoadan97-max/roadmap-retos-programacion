# operadores aritmeticos
      Print(Suma 2+4)
      Print(Resta 10-6)
      Print(Multiplicacion 2*3)
      Print(Division 3/9)
      Print(Modulo 7%3) # es lo que sobra de la division
      Print(Potenciacion 2 ** 3)

# operadores de comparacion
      Print(Igual 5 == 5) True # verifica si son iguales 
      Print(Distinto 3 != 7) True #verifica si son distintos
      Print(Mayor_que 5 > 10) False # verifica si el numero de la izquierda es mayor que el de la derecha
      Print(Menor_que 9 < 7) False # verifiva si el numero de la izquierda es menor que el de la derecha
      Print(Mayor_o_igual_que 7 >= 7) True # verifica si el numero de la izquierda es mayor o igual que el de la derecha
      Print(Menor_o_igual_que 4 <= 4) True # verifica si el numero de la izquierda es menor o igual que el de la derecha

# operadores a nivel bits
      Print(And A = 5 "0101 en binario" B = 3 "0011 en binario" A & B = 1 "0001 en binario")
      Print(Or A | B)
      Print(Xor A ^ B)
      Print(Not A ~ B)
      Print(Desplazamiento_a_la_izquierda A << B)
      Print(desplazzamiento_a_la_derecha A >> B)

#
